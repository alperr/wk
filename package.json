{
  "name": "web-komponents",
  "version": "1.1.7",
  "bin": {
    "wk": "./bin/wk"
  },
  "scripts": {
    "publish": "git add -A && git commit -m 'npm publish' && git push && cp wk.js bin/wk && npm version patch && npm publish"
  }
}
